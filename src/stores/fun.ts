import { atom } from "nanostores";
/**
 * Fun level:
 *
 * 0 - 1, 0 being no fun and 1 being max fun. In the future if I want to expand on this and have ranges from 0 - 5 maybe.
 */
export const fun = atom(0);
